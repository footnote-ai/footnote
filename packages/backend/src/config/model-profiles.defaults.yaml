profiles:
    # ==========
    # OpenAI
    # ==========
    # https://developers.openai.com/api/docs/models/gpt-5.6-luna
    - id: openai-text-fast
      description: Lower-latency, quick replies and lightweight tasks.
      provider: openai
      providerModel: gpt-5.6-luna
      enabled: true
      tierBindings:
          - text-fast
      capabilities:
          canUseSearch: true
          supportedReasoningEfforts: [none, low, medium, high, xhigh, max]
          supportedVerbosity: [low, medium, high]
          toolCapabilities:
              routing.generation.structured-cheap: true
              routing.generation.balanced-general: true
              routing.generation.expressive-generation: false
      maxInputTokens: 1050000
      maxOutputTokens: 128000
      costClass: low
      latencyClass: low

    # https://developers.openai.com/api/docs/models/gpt-5.6-terra
    - id: openai-text-medium
      description: Balanced, everyday conversation and general reasoning.
      provider: openai
      providerModel: gpt-5.6-terra
      enabled: true
      tierBindings:
          - text-medium
      capabilities:
          canUseSearch: true
          supportedReasoningEfforts: [none, low, medium, high, xhigh, max]
          toolCapabilities:
              routing.generation.structured-cheap: true
              routing.generation.balanced-general: true
              routing.generation.expressive-generation: false
      maxInputTokens: 1050000
      maxOutputTokens: 128000
      costClass: medium
      latencyClass: medium

    # https://developers.openai.com/api/docs/models/gpt-5.6-luna
    - id: openai-json-optimized
      description: Structured/JSON-oriented routing profile for planner and assess steps.
      provider: openai
      providerModel: gpt-5.6-luna
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: true
          supportedReasoningEfforts: [none, low, medium, high, xhigh, max]
          toolCapabilities:
              routing.generation.structured-cheap: true
              routing.generation.balanced-general: false
              routing.generation.expressive-generation: false
      maxInputTokens: 1050000
      maxOutputTokens: 128000
      costClass: low
      latencyClass: low

    # https://developers.openai.com/api/docs/models/gpt-5.6-sol
    - id: openai-text-quality
      description: Deeper reasoning and more complex prompts.
      provider: openai
      providerModel: gpt-5.6-sol
      enabled: true
      tierBindings:
          - text-quality
      capabilities:
          canUseSearch: true
          supportedReasoningEfforts: [none, low, medium, high, xhigh, max]
          toolCapabilities:
              routing.generation.structured-cheap: false
              routing.generation.balanced-general: true
              routing.generation.expressive-generation: true
      defaultReasoningEffort: medium
      maxInputTokens: 1050000
      maxOutputTokens: 128000
      costClass: high
      latencyClass: high

    # ==========
    # OpenRouter DeepSeek (manual selection, planning, and presentation)
    # ==========
    # This profile is selected first for normal generation and planning on
    # deployments that provide OpenRouter credentials. The optional presentation
    # flow can reuse the same provider-neutral profile.
    # https://openrouter.ai/deepseek/deepseek-v4-flash-0731
    - id: openrouter-deepseek-v4-flash-0731
      description: OpenRouter DeepSeek profile for normal generation, planning, and optional presentation.
      provider: openrouter
      providerModel: deepseek/deepseek-v4-flash-0731
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
          supportedReasoningEfforts: [low]
      presentationGeneration:
          promptVariant: faithful-rewrite
          reasoningEffort: low
      maxInputTokens: 131072
      maxOutputTokens: 2048
      costClass: low
      latencyClass: high

    # ==========
    # Ollama Cloud
    # ==========
    # https://ollama.com/library/qwen3.5
    - id: ollama-text-qwen
      description: Very long-context, mixed coding and analysis workloads.
      provider: ollama
      providerModel: qwen3.5:cloud
      enabled: true
      tierBindings:
          - text-quality
      capabilities:
          canUseSearch: false
          toolCapabilities:
              routing.generation.structured-cheap: false
              routing.generation.balanced-general: true
              routing.generation.expressive-generation: true
      maxInputTokens: 262144
      # maxOutputTokens:
      costClass: high
      latencyClass: high

    # https://ollama.com/library/gpt-oss:20b-cloud
    - id: ollama-text-gptoss
      description: Solid everyday quality for general chat.
      provider: ollama
      providerModel: gpt-oss:20b-cloud
      enabled: true
      tierBindings:
          - text-medium
      capabilities:
          canUseSearch: false
          toolCapabilities:
              routing.generation.structured-cheap: true
              routing.generation.balanced-general: true
              routing.generation.expressive-generation: false
      maxInputTokens: 131072
      # maxOutputTokens:
      costClass: low
      latencyClass: low

    # ==========
    # Ollama Playground (manual /chat selection only)
    # ==========
    # These profiles are intentionally unbound from text tiers so they do not
    # change default mode routing. They are available for explicit profileId
    # selection while workflows and routing policy continue evolving.
    # Source for model names: https://ollama.com/api/tags and model library pages.
    # Class rubric:
    # - costClass approximates cloud spend from model/compute footprint.
    # - latencyClass approximates turnaround from model size + capability tier.
    - id: ollama-kimi-k2-6
      description: Long-horizon agentic model for complex coding and orchestration tasks.
      provider: ollama
      providerModel: kimi-k2.6
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 262144
      costClass: high
      latencyClass: high

    - id: ollama-glm-5-1
      description: Flagship engineering model with stronger coding and terminal-task performance.
      provider: ollama
      providerModel: glm-5.1
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 262144
      costClass: high
      latencyClass: high

    - id: ollama-gemma4-31b
      description: General-purpose reasoning model with strong long-context performance.
      provider: ollama
      providerModel: gemma4:31b
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 262144
      costClass: high
      latencyClass: high

    - id: ollama-qwen3-5-397b
      description: Very large Qwen variant for demanding reasoning and analysis tasks.
      provider: ollama
      providerModel: qwen3.5:397b
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 262144
      costClass: high
      latencyClass: high

    - id: ollama-qwen3-coder-next
      description: Coding-focused model tuned for repository-scale development workflows.
      provider: ollama
      providerModel: qwen3-coder-next
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 262144
      costClass: medium
      latencyClass: medium

    - id: ollama-ministral-3-14b
      description: Smaller edge-friendly model for quick general-purpose assistance.
      provider: ollama
      providerModel: ministral-3:14b
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 131072
      costClass: low
      latencyClass: low

    - id: ollama-devstral-small-2-24b
      description: Tool-using engineering model for practical multi-file development tasks.
      provider: ollama
      providerModel: devstral-small-2:24b
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 131072
      costClass: medium
      latencyClass: medium

    - id: ollama-nemotron-3-super
      description: Large MoE reasoning model for complex multi-step problem solving.
      provider: ollama
      providerModel: nemotron-3-super
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 262144
      costClass: high
      latencyClass: high

    - id: ollama-qwen3-next-80b
      description: Efficient high-capability model optimized for speed and long context.
      provider: ollama
      providerModel: qwen3-next:80b
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 262144
      costClass: medium
      latencyClass: medium

    - id: ollama-minimax-m2-7
      description: High-end MiniMax model for advanced coding and productivity tasks.
      provider: ollama
      providerModel: minimax-m2.7
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 200000
      costClass: high
      latencyClass: high

    - id: ollama-rnj-1-8b
      description: Compact code-and-STEM-focused model for lightweight technical tasks.
      provider: ollama
      providerModel: rnj-1:8b
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 131072
      costClass: low
      latencyClass: low

    - id: ollama-glm-5
      description: Reasoning and agentic model for complex systems and engineering queries.
      provider: ollama
      providerModel: glm-5
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 262144
      costClass: high
      latencyClass: high

    - id: ollama-nemotron-3-nano-30b
      description: Smaller Nemotron option balancing reasoning quality and efficiency.
      provider: ollama
      providerModel: nemotron-3-nano:30b
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 131072
      costClass: medium
      latencyClass: low

    - id: ollama-minimax-m2-5
      description: Strong coding and agentic model with efficient multi-step execution.
      provider: ollama
      providerModel: minimax-m2.5
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 200000
      costClass: medium
      latencyClass: medium

    - id: ollama-devstral-2-123b
      description: Large tool-using engineering model for ambitious software tasks.
      provider: ollama
      providerModel: devstral-2:123b
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 262144
      costClass: high
      latencyClass: high

    - id: ollama-cogito-2-1-671b
      description: Very large instruction-tuned model for difficult open-ended requests.
      provider: ollama
      providerModel: cogito-2.1:671b
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 262144
      costClass: high
      latencyClass: high

    - id: ollama-glm-4-7
      description: Reasoning and coding-focused GLM model for broad technical queries.
      provider: ollama
      providerModel: glm-4.7
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 131072
      costClass: medium
      latencyClass: low

    - id: ollama-deepseek-v3-2
      description: Reasoning-forward model tuned for agentic and analysis-heavy tasks.
      provider: ollama
      providerModel: deepseek-v3.2
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 131072
      costClass: high
      latencyClass: high

    - id: ollama-minimax-m2
      description: Balanced MiniMax model for general coding and productivity use.
      provider: ollama
      providerModel: minimax-m2
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 200000
      costClass: medium
      latencyClass: low

    - id: ollama-minimax-m2-1
      description: Multilingual MiniMax variant with emphasis on code engineering.
      provider: ollama
      providerModel: minimax-m2.1
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 200000
      costClass: medium
      latencyClass: low

    - id: ollama-gpt-oss-120b
      description: Open-weight high-capability reasoning model for demanding text tasks.
      provider: ollama
      providerModel: gpt-oss:120b
      enabled: true
      tierBindings: []
      capabilities:
          canUseSearch: false
      maxInputTokens: 131072
      costClass: high
      latencyClass: high

pools:
    free-ollama-style:
        - ollama-text-qwen
        - ollama-text-gptoss
        - ollama-ministral-3-14b
        - ollama-rnj-1-8b
        - ollama-nemotron-3-nano-30b

# User-configurable per-step routing chains.
# - planner/generate/assess are resolved independently for each mode.
# - string entry: direct profile id or pool id.
# - chooseOne: deterministic equal-chance pick keyed by session/correlation seed.
# - invalid ids/entries are skipped fail-open at runtime config load.
stepRoutingChains:
    express:
        planner:
            # Intent: keep planning on the configured OpenRouter model first.
            - openrouter-deepseek-v4-flash-0731
            # Fallback: retain the OpenAI structured planner when available.
            - openai-json-optimized
            - ollama-text-gptoss
        generate:
            # Intent: prefer the configured OpenRouter main model when available.
            - openrouter-deepseek-v4-flash-0731
            # Fallback: preserve the existing express lane if the provider fails.
            - openai-text-fast
            # Fallback: keep one low-cost pooled option available.
            - chooseOne: [free-ollama-style]
        assess:
            # Intent: keep assess deterministic when a reviewed path is active.
            - openai-json-optimized
            - ollama-text-gptoss
    balanced:
        planner:
            # Intent: keep planning on the configured OpenRouter model first.
            - openrouter-deepseek-v4-flash-0731
            # Fallback: retain the OpenAI structured planner when available.
            - openai-json-optimized
            - ollama-text-gptoss
        generate:
            # Intent: prefer the configured OpenRouter main model when available.
            - openrouter-deepseek-v4-flash-0731
            # Fallback: preserve the existing balanced routing chain.
            - chooseOne: [free-ollama-style]
            # Fallback: stronger deterministic quality pass when ollama path fails.
            - openai-text-medium
        assess:
            # Intent: keep review logic structured/compact; fail-open to ollama.
            - openai-json-optimized
            - ollama-text-gptoss
    grounded:
        planner:
            # Intent: keep grounded planning on the configured OpenRouter model first.
            - openrouter-deepseek-v4-flash-0731
            # Fallback: retain the OpenAI structured planner when available.
            - openai-json-optimized
            - ollama-text-gptoss
        generate:
            # Intent: prefer the configured OpenRouter main model when available.
            - openrouter-deepseek-v4-flash-0731
            # Fallback: preserve the existing grounded routing chain.
            - openai-text-medium
            # Fallback: lower-cost style/creativity pass from free ollama pool.
            - chooseOne: [free-ollama-style]
        assess:
            # Intent: grounded assessment stays logic-first and schema-friendly.
            - openai-json-optimized
            - ollama-text-gptoss
