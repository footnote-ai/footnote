name: footnote-server-dev-build

services:
    server:
        build:
            context: ..
            dockerfile: deploy/Dockerfile.server
        # Local source build default tag; set FOOTNOTE_IMAGE_REF to a GHCR ref when pulling.
        image: ${FOOTNOTE_IMAGE_REF:-footnote:dev-local}
        env_file:
            - ../.env
        environment:
            PORT: '3000'
        ports:
            - '8080:3000'
        volumes:
            - footnote_data:/data
            - ../footnote.yaml:/data/config/footnote.yaml:ro
        restart: unless-stopped

volumes:
    footnote_data:
